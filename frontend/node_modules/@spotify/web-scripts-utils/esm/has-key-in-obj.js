export var hasKeyInObj = function (key, obj) {
    if (obj === void 0) { obj = {}; }
    var _a = key.split('.'), k = _a[0], restOfK = _a.slice(1);
    if (restOfK.length === 0) {
        return Object.keys(obj).some(function (ok) { return ok === k; });
    }
    var nextObj = obj[k];
    if (!(nextObj instanceof Object)) {
        return false;
    }
    return hasKeyInObj(restOfK.join('.'), nextObj);
};
