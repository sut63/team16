// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/G16/app/ent/age"
	"github.com/facebookincubator/ent/dialect/sql"
)

// Age is the model entity for the Age schema.
type Age struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// AGE holds the value of the "AGE" field.
	AGE int `json:"AGE,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AgeQuery when eager-loading is set.
	Edges AgeEdges `json:"edges"`
}

// AgeEdges holds the relations/edges for other nodes in the graph.
type AgeEdges struct {
	// Employee holds the value of the employee edge.
	Employee []*Employee
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// EmployeeOrErr returns the Employee value or an error if the edge
// was not loaded in eager-loading.
func (e AgeEdges) EmployeeOrErr() ([]*Employee, error) {
	if e.loadedTypes[0] {
		return e.Employee, nil
	}
	return nil, &NotLoadedError{edge: "employee"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Age) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // id
		&sql.NullInt64{}, // AGE
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Age fields.
func (a *Age) assignValues(values ...interface{}) error {
	if m, n := len(values), len(age.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	a.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullInt64); !ok {
		return fmt.Errorf("unexpected type %T for field AGE", values[0])
	} else if value.Valid {
		a.AGE = int(value.Int64)
	}
	return nil
}

// QueryEmployee queries the employee edge of the Age.
func (a *Age) QueryEmployee() *EmployeeQuery {
	return (&AgeClient{config: a.config}).QueryEmployee(a)
}

// Update returns a builder for updating this Age.
// Note that, you need to call Age.Unwrap() before calling this method, if this Age
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Age) Update() *AgeUpdateOne {
	return (&AgeClient{config: a.config}).UpdateOne(a)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (a *Age) Unwrap() *Age {
	tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Age is not a transactional entity")
	}
	a.config.driver = tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Age) String() string {
	var builder strings.Builder
	builder.WriteString("Age(")
	builder.WriteString(fmt.Sprintf("id=%v", a.ID))
	builder.WriteString(", AGE=")
	builder.WriteString(fmt.Sprintf("%v", a.AGE))
	builder.WriteByte(')')
	return builder.String()
}

// Ages is a parsable slice of Age.
type Ages []*Age

func (a Ages) config(cfg config) {
	for _i := range a {
		a[_i].config = cfg
	}
}
